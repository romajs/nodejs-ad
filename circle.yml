machine:
  node:
    version: 6.1.0
dependencies:
  override:
    - npm install
test:
  # pre:
    # - npm run lint
  override:
    - npm test
  # post:
  #   - npm run coverage
  #   - npm run codecov