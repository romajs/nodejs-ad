machine:
  node:
    version: 6.1.0
  services:
    - docker

# dependencies:
#   override:
#     - sudo pip install --upgrade docker-compose==1.6.0

dependencies:
  override:
    - npm install
test:
  pre:
    # - cd ./api/src && npm run lint
  override:
    - cd ./api/src && npm test
  post:
    - cd ./api/src && npm run coverage
    - cd ./api/src && npm run codecov