machine:
  node:
    version: 6.1.0
  services:
    - docker

dependencies:
  override:
    - sudo pip install --upgrade docker-compose==1.6.0

dependencies:
  override:
    - npm install
test:
  pre:
    - sudo service mongodb stop
    # - npm run lint
  override:
    - npm test
  # post:
  #   - npm run coverage
  #   - npm run codecov